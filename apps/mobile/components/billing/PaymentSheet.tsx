// @stripe/stripe-react-native wrapper. Calls
// /api/v1/billing/mobile-subscription then initPaymentSheet with
// customerId + ephemeralKeySecret + paymentIntentClientSecret. STUB.
export function PaymentSheet() { return null }
