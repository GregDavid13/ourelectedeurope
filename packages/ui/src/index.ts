// Shared React web primitives. STUB — populate with shadcn/ui-derived
// components shared across apps/web (and any future web surfaces).
// Keep platform-agnostic primitives here; app-specific composition
// stays in apps/web/components.
export {}
