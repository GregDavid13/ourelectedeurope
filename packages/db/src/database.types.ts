// GENERATED FILE — do not edit by hand.
// Regenerate after every migration (and before relying on types):
//   supabase gen types typescript --local > packages/db/src/database.types.ts
//
// ⚠️ PERMISSIVE PLACEHOLDER. Until you run the command above, the DB
// is effectively UNTYPED — every query returns `any`, ZERO column
// safety. This is deliberate: any stricter placeholder makes
// supabase-js's select/insert generics collapse to `never` and breaks
// the build before codegen. Generating real types is a required
// bootstrap step (see CLAUDE.md / SCAFFOLD-NOTES.md), not optional.
/* eslint-disable @typescript-eslint/no-explicit-any */

export type Database = any
